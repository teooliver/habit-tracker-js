import styled from "styled-components";

const HabitFormStyles = styled.div`
  margin: 1.2rem;
  label {
    margin: 0 0.3rem;
  }
  input {
    vertical-align: middle;
  }
`;

export default HabitFormStyles;
